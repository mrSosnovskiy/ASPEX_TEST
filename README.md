# ASPEX_TEST
## Выполнение тестового задания для ASPEX
Для выполнения был развернут локальный сервер mssql в докере 

```
docker pull mcr.microsoft.com/mssql/server:2025-latest - затянул образ
docker run -e "ACCEPT_EULA=Y" -e "MSSQL_SA_PASSWORD=*********"  -p 1433:1433 --name sql --hostname sql -d mcr.microsoft.com/mssql/server:2025-latest - запустил контейнер
```

Подключился через DBeaver 

В репозиторий прикрепляю SQL скрипты выполнения заданий

Ответ на последний вопрос по автоматизации загрузки данных в  ASPEX_TEST_PROC_ANSWER_LAST_Q.SQl

Продублирую и сюда 

--Исходя из своего опыта могу предположить что автоматизировать процесс загрузки можно с помощью инструментов аркестрации загрузки к премеру Apache Airflow в котором мы можем прописать TASK  с вызовом хранимых процедур и функци, вызывать их по времени или с помощью сенсоров

--то есть после успешного завершения предыдущей задачи оркестратора;
